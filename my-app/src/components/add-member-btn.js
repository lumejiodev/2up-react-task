import React, { Component } from 'react';

class AddMemberBtn extends Component {
    render () {
        return (
            <button type="submit" className="add-member__btn">Добавить</button>
        );
    }
}

export default AddMemberBtn;